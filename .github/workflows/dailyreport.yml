name: daily Report

on:
  workflow_dispatch:
  # push:
  #   branches:
  #     - main

env:
  USR_EM: lola.berserker99@gmail.com
  USR_PSS: LolaBerserker@13

jobs:
  send_email:
    runs-on: ubuntu-latest
    steps:
      - name: Start Process
        run: echo "Process Starting"

      - uses: actions/checkout@v2

      - uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Run Email Code
        env:
          USER_EMAIL: ${USR_EM}
          USER_PASSWORD: ${USR_PSS}
        run: python send_email.py
